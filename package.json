{
  "name": "repro_emotion_bug",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean:output": "rimraf dist && rimraf publish",
    "start": "npm run clean:output && parcel watch manifest.json --host localhost --port 9232",
    "build": "npm run clean:output && parcel build manifest.json --no-source-maps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@parcel/config-webextension": "^2.8.1",
    "@parcel/transformer-typescript-tsc": "^2.8.1",
    "parcel": "^2.8.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-select": "^5.7.0",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "process": "^0.11.10"
  }
}
